name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  plugin:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - name: Test
        run: ./gradlew test
      - name: Build sample fixture
        run: ./gradlew -p samples/kmp-build-fixture compileKotlinJvm
      - name: Build plugin
        run: ./gradlew buildPlugin
